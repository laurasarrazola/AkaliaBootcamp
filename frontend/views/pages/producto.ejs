<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <title>Contáctanos | Akalia</title>
    <%- include('../partials/head.ejs'); %>
</head>

<body class="h-100 m-0 d-flex flex-column vh-100">
    <%- include('../partials/navbar.ejs'); %>
        <main class="container py-5">
            <div class="row bg-blanco-light rounded shadow-sm p-4">
                <!-- Miniaturas -->
                <div class="col-2 d-flex flex-column align-items-center gap-2">
                    <% imagenes.forEach(function(img, i) { %>
                        <img src="<%= img.urlImagen %>" class="img-thumbnail mb-2"
                            style="width: 70px; height: 70px; object-fit: cover; cursor:pointer;"
                            alt="Miniatura <%= i+1 %>">
                        <% }) %>
                </div>
                <!-- Imagen principal + Info del artesano + Redes sociales -->
                <div class="col-5 d-flex flex-column align-items-center justify-content-center">
                    <img src="<%= imagenes[1].urlImagen %>" class="img-fluid rounded mb-3"
                        style="max-height:350px; object-fit:cover;" alt="<%= producto.nombre %>">

                    <!-- Hecho por -->
                    <div class="mb-3 d-flex align-items-center gap-2">
                        <img src="/img/icons/hand.svg" alt="Hecho por" style="width:24px;">
                        <span class="text-dark">Hecho por <%= emprendimiento.nombreEmprendimiento %></span>
                    </div>

                    <!-- Redes sociales -->
                    <div class="mb-3 text-center">
                        <span class="fw-semibold text-verde d-block">Redes Sociales</span>
                        <div class="d-flex flex-wrap gap-3 justify-content-center mt-2">
                            
                            <% if(redSocial.nombreRedSocial[0]) { %>
                                <span><i class="fab fa-whatsapp"></i> <a
                                        href="https://wa.me/<%= redSocial.url[0] %>" target="_blank">whatsapp/<%=
                                            redSocial.url %></a></span>
                                <% } %>
                            <% if(redSocial.nombreRedSocial[1]) { %>
                                <span><i class="fab fa-facebook"></i> <a
                                                href="https://facebook.com/<%= redSocial.facebook %>"
                                                target="_blank">facebook/<%= redSocial.url[1] %></a></span>
                                <% } %>
                            <% if(redSocial.nombreRedSocial[2]) { %>
                                <span><i class="fab fa-instagram"></i> <a
                                                href="https://instagram.com/<%= redSocial.instagram %>"
                                                target="_blank">instagram/<%= redSocial.url[2] %>
                                </a></span>
                                <% } %>
                            <div class="mt-4 d-flex align-items-center gap-2">
                    <i class="fas fa-share-alt"></i>
                    <button class="btn btn-explorar" onclick="navigator.clipboard.writeText(window.location.href)">Compartir publicación</button>
                </div>
                        </div>
                    </div>
                </div>
                <!-- Info producto -->
                <div class="col-5">
                <h2 class="fw-bold mb-2"><%= producto.tituloProducto %></h2>
                <h3 class="text-vinotinto mb-3">$ <%= producto.precio %></h3>
                <h5 class="mb-2">Descripción:</h5>
                <p class="mb-3"><%= producto.descripcionProducto %></p>
                <div class="mb-3">
                    <span class="fw-semibold text-muted">Categoría:</span>
                    <% if (categoriaP && categoriaP.length) { %>
                        <% categoriaP.forEach(categoria => { %>
                            <span class="text-dark"><%= categoria.nombreCategoria %></span>)
                        <% }); %>
                     <% } else { %>
                    <span class="text-dark">Sin categoría</span>
                    <% } %>
                </div>
                
                </div>
            </div>
        </main>
        <%- include('../partials/footer.ejs'); %>
</body>

</html>